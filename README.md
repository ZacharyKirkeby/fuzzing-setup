# libmodbus-fuzzing
fuzzing setup / seed files

This exists to make my life easier with some fuzzing work and transfering files between machines

FFMPEG - contains test harness, dictionary, seed corpus, and build scripts (to be run as root) handling full setup 
running. Specifically written for my arch machine. 

LibModbus - not fully functional, seed files, dictionary, some half functional build files
